# Exact-date evidence classification cache

These JSON files are **manually curated / validated offline classifications**
used by the fail-closed evidence replay path (`src/evidence/research_preview.py`).

They are not live retrieval results and are not debug dumps.
They must match the versioned local corpus under `data/corpus/`.
