.venv/
.uv-cache/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.DS_Store
.ipynb_checkpoints/
.env
.env.*

# Optional DeepSeek explanation cache (regenerable; not part of the frozen demo).
outputs/gdelt_evidence_cache/
outputs/llm_cache/

# Alternative-data raw caches.
# Cached locally so later sessions make zero network calls; too large for git.
# Provenance remains in committed manifests under data/raw/*/ and processed extracts.
data/raw/finra/daily/
data/raw/prices/
data/raw/sp500/prices/
data/raw/sp500/benchmark/
data/raw/gdelt/*.json
!data/raw/gdelt/*.metadata.json
!data/raw/gdelt/acquisition_report.json
data/raw/sec/company_facts_CIK*.json

# Local SEC feasibility extracts (not part of the PM-facing submission path).
outputs/fundamental_alignment/

# Reference papers. Read locally, cited by number in the memo, never
# redistributed: this repository is public and the working papers are not
# ours to republish. Daniel-Moskowitz (2016) is NBER w20439.
momentum crash paper/
*.pdf
